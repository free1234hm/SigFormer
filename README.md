# SigFormer
SigFormer is a graph Transformer-based framework for reconstructing transcellular signaling pathways. It uses scRNA-seq as the required input and can optionally integrate multi-omics and spatial data (if available) to refine pathway inference.
